/*Aca va la funcion que se pide transformar  a una funcion de expresion*/
function example(a, b, c){
    return a+b+c
    }

    /* Aca va mi respuesta */
    let example = function(a, b, c){
        return a+b+c
    }